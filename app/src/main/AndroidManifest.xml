<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_lab4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_lab4">
        <activity
            android:name=".ui.UserNameFormActivity"
            android:label="@string/user_name_form"
            android:exported="true"/>
        <activity
            android:name=".ui.AddressFormActivity"
            android:label="@string/address_form"
            android:exported="true"/>
        <activity
            android:name=".ui.CommentFormActivity"
            android:label="@string/comment_form"
            android:exported="true"/>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>